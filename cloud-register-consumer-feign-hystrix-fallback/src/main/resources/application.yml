server:
  port: 8017

spring:
  application:
    name: cloud-register-consumer-feign-hystrix-fallback

feign:
  hystrix:
    enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8001/eureka/
  instance:
    prefer-ip-address: true


